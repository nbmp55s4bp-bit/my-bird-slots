<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Tropical Paradise Slots</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        :root {
            --gold: #ffd700;
            --jungle: #2e8b57;
            --dark: #1a1a1a;
        }
        body {
            font-family: 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(180deg, #0f2027, #203a43, #2c5364);
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            color: white;
            height: 100vh;
            overflow: hidden;
        }
        .header h1 {
            font-size: 28px;
            margin-top: 15px;
            text-shadow: 0 0 10px var(--gold);
            letter-spacing: 2px;
        }
        .balance-card {
            background: rgba(0,0,0,0.7);
            padding: 15px 30px;
            border-radius: 15px;
            border: 2px solid var(--gold);
            text-align: center;
            margin-bottom: 15px;
        }
        .balance-amount {
            font-size: 32px;
            font-weight: bold;
            color: var(--gold);
        }
        .wallet-btns {
            display: flex;
            gap: 10px;
            margin-top: 10px;
        }
        .wallet-btns button {
            padding: 5px 15px;
            border-radius: 5px;
            border: none;
            font-weight: bold;
            font-size: 12px;
        }
        .dep-btn { background: #4caf50; color: white; }
        .wd-btn { background: #f44336; color: white; }

        .slot-machine {
            background: #4a3000;
            padding: 10px;
            border-radius: 15px;
            border: 4px solid #b8860b;
            box-shadow: 0 0 30px rgba(0,0,0,0.8);
        }
        .slot-grid {
            display: grid;
            grid-template-columns: repeat(3, 80px);
            grid-template-rows: repeat(3, 80px);
            gap: 5px;
            background: #111;
            padding: 5px;
            border-radius: 10px;
        }
        .cell {
            background: white;
            border-radius: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 45px;
            color: #000;
            transition: 0.1s;
        }
        .spinning {
            animation: roll 0.1s infinite linear;
        }
        @keyframes roll {
            0% { transform: translateY(-5px); filter: blur(2px); }
            100% { transform: translateY(5px); filter: blur(5px); }
        }
        .controls { margin-top: 25px; }
        .spin-btn {
            background: linear-gradient(180deg, #ffeb3b, #fbc02d);
            border: none;
            padding: 18px 60px;
            font-size: 26px;
            font-weight: bold;
            border-radius: 50px;
            color: #4a3000;
            box-shadow: 0 6px 0 #c49000;
            cursor: pointer;
        }
        .spin-btn:active { transform: translateY(4px); box-shadow: 0 2px 0 #c49000; }
        .spin-btn:disabled { filter: grayscale(1); opacity: 0.7; }

        .status-msg {
            margin-top: 15px;
            height: 20px;
            font-weight: bold;
            color: #00ff00;
        }
    </style>
</head>
<body>

    <div class="header"><h1>BIRD SLOTS 🦜</h1></div>

    <div class="balance-card">
        <div class="balance-amount">💰 <span id="balance">1000</span></div>
        <div class="wallet-btns">
            <button class="dep-btn" onclick="sendData('deposit')">ПОПОВНИТИ</button>
            <button class="wd-btn" onclick="sendData('withdraw')">ВИВЕСТИ</button>
        </div>
    </div>

    <div class="slot-machine">
        <div class="slot-grid" id="grid">
            <div class="cell">🦜</div><div class="cell">🦉</div><div class="cell">🦩</div>
            <div class="cell">💎</div><div class="cell">🎰</div><div class="cell">🍍</div>
            <div class="cell">🦜</div><div class="cell">🐦</div><div class="cell">🦉</div>
        </div>
    </div>

    <div class="status-msg" id="status">Зробіть ставку (10 💰)</div>

    <div class="controls">
        <button class="spin-btn" id="spinBtn" onclick="spin()">SPIN</button>
    </div>

    <script>
        const tg = window.Telegram.WebApp;
        tg.expand();

        let balance = 1000;
        const symbols = ["🦜", "🦉", "🦩", "🐦", "🍍", "💎", "🎰"];
        const grid = document.getElementById('grid');
        const cells = document.querySelectorAll('.cell');
        const status = document.getElementById('status');

        function spin() {
            if (balance < 10) {
                status.textContent = "Недостатньо коштів!";
                return;
            }

            balance -= 10;
            updateUI();
            
            document.getElementById('spinBtn').disabled = true;
            status.textContent = "Крутимо...";
            if (tg.HapticFeedback) tg.HapticFeedback.impactOccurred('medium');

            cells.forEach(c => c.classList.add('spinning'));

            setTimeout(() => {
                let results = [];
                cells.forEach(c => {
                    c.classList.remove('spinning');
                    const s = symbols[Math.floor(Math.random() * symbols.length)];
                    c.textContent = s;
                    results.push(s);
                });

                checkWin(results);
                document.getElementById('spinBtn').disabled = false;
            }, 1200);
        }

        function checkWin(res) {
            let win = 0;
            // Перевірка горизонтальних ліній
            if (res[0] === res[1] && res[1] === res[2]) win += 50;
            if (res[3] === res[4] && res[4] === res[5]) win += 100;
            if (res[6] === res[7] && res[7] === res[8]) win += 50;

            if (win > 0) {
                balance += win;
                status.textContent = `ВИГРАШ +${win} 💰!`;
                if (tg.HapticFeedback) tg.HapticFeedback.notificationOccurred('success');
            } else {
                status.textContent = "Спробуй ще!";
            }
            updateUI();
        }

        function updateUI() {
            document.getElementById('balance').textContent = balance;
        }

        function sendData(action) {
            const data = {
                action: action,
                balance: balance,
                user: tg.initDataUnsafe?.user?.username || "Gamer"
            };
            tg.sendData(JSON.stringify(data));
        }
    </script>
</body>
</html>
